# =============================================================================================
# Boeing 737-800 -- full mission sizing, flown on OpenConcept's OpenAeroStruct lattice
# =============================================================================================
# The same aeroplane, mission and reserves as b738.yaml. The one difference is where the
# lift-dependent drag comes from: a vortex lattice built on this wing, instead of the parabolic
# polar `ac|aero|polar|e` describes.
#
# `ac|aero|polar|e` is still declared below and is deliberately NOT consulted by this run. The
# lattice computes the span efficiency rather than reading one, and reports about 0.990 for this
# planform against the 0.801 stated -- which is the whole reason to run this case beside
# b738.yaml. See docs/aerodynamics.rst for what that is and is not evidence of, and docs/truth.rst
# for what the lattice is validated against.
# =============================================================================================

black_box:
  # cdadt's own analysis group, not OpenConcept's. That is what makes the substitution possible:
  # B738_sizing.py writes `aircraft_model=B738AircraftModel` at the line where it builds the
  # mission, so the aerodynamics cannot be reached from outside that group. Everything else here
  # is still OpenConcept's, composed exactly as its own example composes it -- the trapezoidal
  # geometry, the tail volume-coefficient sizing, the maximum-lift estimates, the jet-transport
  # empty-weight buildup, and FullMissionWithReserve with its balanced field and Part 25 reserves.
  model: cdadt.adapter.analysis:SizingMissionAnalysis
  options:
    # The induced drag comes from OpenConcept's own OpenAeroStruct vortex lattice, on a mesh
    # built from the same four wing numbers openavl's is. The point of running this beside the
    # openavl case is that the two are independent codes: compared like for like they agree on
    # this wing to 0.67%, so a difference in the sized aeroplane is evidence about a modelling
    # choice rather than about one solver.
    #
    # Two differences are declared rather than smoothed over. Its VLM publishes only near-field
    # induced drag, so it implies a span efficiency above 1 -- impossible for a planar wing --
    # and runs about 4% optimistic. And its polar is a fit with a 2.2e-3 residual, where
    # openavl's is an identity to 1e-12. See docs/truth.rst.
    #
    # Requires the optional extra:  pip install -e ".[transonic]"
    aerodynamic_loads: cdadt.adapter.oas:OpenAeroStructLoads
    # Transonic drag rise is ON. The mission cruises at M 0.7854 and touches it in climb and
    # descent, and at that Mach the Korn model puts wave drag at 2.2% of total cruise drag on this
    # wing -- rising to 11.6% at the M 0.82 the case file declares as Mach_max. Neither the vortex
    # lattice nor OpenConcept's parasite buildup carries a Mach term, so with this off the study
    # flies a transonic aeroplane with no drag rise whatsoever.
    #
    # It also changes the answer qualitatively, not just quantitatively: with it off, sweep is a
    # variable the optimizer can only lose by, and it drives quarter-chord sweep to its lower bound.
    # With it on, sweep goes the other way, to about 31 degrees.
    #
    # Requires OpenAeroStruct, which is where OpenConcept keeps the component.
    wave_drag: true
  num_nodes: 21          # must be odd: the box integrates fuel burn with Simpson's rule

solver:
  maxiter: 20
  atol: 1.0e-9
  rtol: 1.0e-9
  iprint: -1
  err_on_non_converge: true

# ---------------------------------------------------------------------------------------------
# Design variables
# ---------------------------------------------------------------------------------------------
# Every ac| variable the box publishes, with its value and where the number came from. None is
# freed here: this is a sizing run. See b738_optimization.yaml for the same aeroplane with
# `optimize:` entries added.
design_variables:

  # -------------------- Aerodynamics --------------------
  ac|aero|polar|e:
    value: 0.801
    source: OpenConcept B738 example estimate
  ac|aero|Mach_max:
    value: 0.82
    source: Boeing 737-800 maximum operating Mach
  ac|aero|Vstall_land:
    value: 115
    units: kn
    source: OpenConcept B738_sizing estimate
  ac|aero|airfoil_Cl_max:
    value: 1.75
    source: OpenConcept B738_sizing estimate for a supercritical airfoil
  ac|aero|takeoff_flap_deg:
    value: 15
    units: deg
    source: Boeing 737-800 normal takeoff flap setting

  # -------------------- Propulsion --------------------
  ac|propulsion|engine|rating:
    value: 27.0e3
    units: lbf
    source: CFM56-7B27 sea-level static thrust
  ac|propulsion|num_engines:
    value: 2
    source: Boeing 737-800 configuration

  # -------------------- Wing --------------------
  ac|geom|wing|S_ref:
    value: 124.6
    units: m**2
    source: b737.org.uk technical specifications
  ac|geom|wing|AR:
    value: 9.45
    source: b737.org.uk technical specifications
  ac|geom|wing|c4sweep:
    value: 25
    units: deg
    source: b737.org.uk technical specifications
  ac|geom|wing|taper:
    value: 0.159
    source: b737.org.uk technical specifications
  ac|geom|wing|toverc:
    value: 0.12
    source: OpenConcept B738_sizing estimate

  # -------------------- Horizontal stabilizer --------------------
  # Its area is absent on purpose: the box sizes it from the tail volume coefficient.
  ac|geom|hstab|AR:
    value: 6.16
    source: b737.org.uk technical specifications
  ac|geom|hstab|c4sweep:
    value: 30
    units: deg
    source: b737.org.uk technical specifications
  ac|geom|hstab|taper:
    value: 0.203
    source: b737.org.uk technical specifications
  ac|geom|hstab|toverc:
    value: 0.12
    source: OpenConcept B738_sizing estimate

  # -------------------- Vertical stabilizer --------------------
  ac|geom|vstab|AR:
    value: 1.91
    source: b737.org.uk technical specifications
  ac|geom|vstab|c4sweep:
    value: 35
    units: deg
    source: b737.org.uk technical specifications
  ac|geom|vstab|taper:
    value: 0.271
    source: b737.org.uk technical specifications
  ac|geom|vstab|toverc:
    value: 0.12
    source: OpenConcept B738_sizing estimate

  # -------------------- Fuselage --------------------
  ac|geom|fuselage|length:
    value: 38.08
    units: m
    source: b737.org.uk technical specifications
  ac|geom|fuselage|height:
    value: 3.76
    units: m
    source: b737.org.uk technical specifications

  # -------------------- Nacelle --------------------
  ac|geom|nacelle|length:
    value: 4.3
    units: m
    source: OpenConcept B738_sizing photogrammetry estimate
  ac|geom|nacelle|diameter:
    value: 2
    units: m
    source: OpenConcept B738_sizing photogrammetry estimate

  # -------------------- Landing gear --------------------
  ac|geom|maingear|length:
    value: 1.8
    units: m
    source: OpenConcept B738_sizing estimate
  ac|geom|maingear|num_wheels:
    value: 4
    source: Boeing 737-800 configuration
  ac|geom|maingear|num_shock_struts:
    value: 2
    source: Boeing 737-800 configuration
  ac|geom|nosegear|length:
    value: 1.3
    units: m
    source: OpenConcept B738_sizing estimate
  ac|geom|nosegear|num_wheels:
    value: 2
    source: Boeing 737-800 configuration

  # -------------------- Weights, cabin and occupancy --------------------
  ac|weights|W_payload:
    value: 18.0e3
    units: kg
    source: OpenConcept B738_sizing design payload
  ac|num_passengers_max:
    value: 189
    source: Boeing 737-800 single-class maximum
  ac|num_flight_deck_crew:
    value: 2
    source: Boeing 737-800 configuration
  ac|num_cabin_crew:
    value: 4
    source: Boeing 737-800 configuration
  ac|cabin_pressure:
    value: 8.95
    units: psi
    source: Boeing 737-800 cabin differential

# ---------------------------------------------------------------------------------------------
# Initial conditions
# ---------------------------------------------------------------------------------------------
# The direct counterpart of `set_values(prob, num_nodes)`, with the same names. A two-element
# value is interpolated across the phase, exactly as `np.linspace(2300.0, 400.0, num_nodes)`
# does there; a single value is constant; a full-length list is used as given.
initial_conditions:

  # -------------------- Mission-level --------------------
  mission_range: {value: 2800, units: nmi}
  cruise|h0:     {value: 35000, units: ft}
  reserve_range: {value: 200, units: nmi}
  reserve|h0:    {value: 15000, units: ft}

  # -------------------- Per-phase speed and vertical speed --------------------
  climb.fltcond|Ueas:           {value: [230, 252], units: kn}
  climb.fltcond|vs:             {value: [2300, 400], units: ft/min}
  cruise.fltcond|Ueas:          {value: 252, units: kn}
  cruise.fltcond|vs:            {value: 0, units: ft/min}
  descent.fltcond|Ueas:         {value: [252, 250], units: kn}
  descent.fltcond|vs:           {value: [-1300, -800], units: ft/min}
  reserve_climb.fltcond|Ueas:   {value: 230, units: kn}
  reserve_climb.fltcond|vs:     {value: [3000, 2300], units: ft/min}
  reserve_cruise.fltcond|Ueas:  {value: 250, units: kn}
  reserve_cruise.fltcond|vs:    {value: 0, units: ft/min}
  reserve_descent.fltcond|Ueas: {value: 250, units: kn}
  reserve_descent.fltcond|vs:   {value: -800, units: ft/min}
  loiter.fltcond|Ueas:          {value: 250, units: kn}
  loiter.fltcond|vs:            {value: 0, units: ft/min}

  # -------------------- Solver starting points --------------------
  # The ground-roll phases integrate acceleration from a standstill, so their solver needs a
  # true-airspeed guess rather than a schedule. Maximum takeoff weight is an *output* the box
  # solves for; writing it here is choosing where Newton starts, not choosing the weight.
  v0v1.fltcond|Utrue: {value: 100, units: kn}
  v1vr.fltcond|Utrue: {value: 100, units: kn}
  v1v0.fltcond|Utrue: {value: 100, units: kn}
  ac|weights|MTOW:    {value: 50.0e3, units: kg}

# ---------------------------------------------------------------------------------------------
# Continuation
# ---------------------------------------------------------------------------------------------
# The design mission does not converge from a cold start. Each rung is converged in turn, so the
# solver enters the next from a converged neighbour. This is the ladder B738_sizing.py walks
# between its two `run_model()` calls, written as data rather than as statements.
continuation:
  - description: short range at low altitude, gentle descent
    initial_conditions:
      mission_range:      {value: 500, units: nmi}
      cruise|h0:          {value: 5000, units: ft}
      reserve_range:      {value: 100, units: nmi}
      reserve|h0:         {value: 1000, units: ft}
      descent.fltcond|vs: {value: -800, units: ft/min}

  - description: design range and altitude, still with the gentle descent
    initial_conditions:
      descent.fltcond|vs: {value: -800, units: ft/min}
